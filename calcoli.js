/*Clcoli per la app sul BMI */
function calcola(){
// Identificare i dati del form 
var peso=document.forms.BMI.peso.value;
var altezza=var peso=document.forms.BMI.altezza.value;
var sesso=document.forms.BMI.sesso.value;
var totale=pow(base,exp)
// verifico se l'utente è m o f 
if(sesso='m'){
/ applico la formula BMI per m
var res=;
       }
else{
// applico formula per BMI per f 
var totale=
}
var risultato=getElemntById('risultato')
risultato.innerHTML="BMI ="+"res;
}
function annulla(){

}
